import HashtagView from "../components/Hashtag/HashtagView";

/**
 * Route pour la page des hashtags.
 * Sert de point d'entrée pour le composant HashtagView.
 */
export default function Hashtag() {
  return <HashtagView />;
}
